// Theme tokens have been migrated to tailwind.config.js
// This file is kept for reference and can be safely deleted.

export const theme = {
  colors: {
    primary: '#0B3D91',
    secondary: 'rgb(96, 0, 199)',
    tertiary: '#4D7C8A',
    accent: '#00D8FF',
  },
};

export type Theme = typeof theme;
export default theme;
